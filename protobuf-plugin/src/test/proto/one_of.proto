syntax = "proto3";

package kotlinx.rpc.protobuf.test;

import "all_primitives.proto";
import "reference_package.proto";

message OneOf {
  oneof primitives {
    string string_value = 1;
    int32 int32 = 2;
    bool bool = 3;
  }

  oneof references {
    Other other = 4;
    References inner_references = 5;
  }

  oneof mixed {
    int64 int64 = 6;
    AllPrimitives allPrimitives = 7;
  }

  oneof single {
    bytes bytes = 8;
  }
}
